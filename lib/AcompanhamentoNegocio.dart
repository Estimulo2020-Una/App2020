import 'package:flutter/material.dart';

class Acompanhamento extends StatelessWidget{
  build(context){
    return MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text("Essa e a tela de reports"),
        )
      ),
    );
  }
}