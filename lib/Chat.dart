import 'package:flutter/material.dart';

class Chat extends StatelessWidget{
  build(context){
    return MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text("Esse e o chat"),
        )
      ),
    );
  }
}